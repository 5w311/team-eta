{
  "name": "milespost",
  "version": "2.1.6",
  "private": true,
  "description": "Team-driver ETA and 34-hour reset PWA. Runs offline.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:browser": "node test/smoke.browser.mjs"
  },
  "devDependencies": {
    "playwright": "^1.61.1",
    "vitest": "^2.1.8"
  }
}
