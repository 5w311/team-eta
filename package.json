{
  "name": "milespost",
  "version": "2.0.1",
  "private": true,
  "description": "Team-driver ETA and 34-hour reset PWA. Runs offline.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
